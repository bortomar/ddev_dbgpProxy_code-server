{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "DDEV: Enable Xdebug",
			"type": "shell",
			"command":  "[ ! $(command -v docker) ] && xdebug on || ddev xdebug on", 
			"presentation": {
				"reveal": "silent",
				"close": true
			}
		},
		{
			"label": "DDEV: Disable Xdebug",
			"type": "shell",
			"command": "[ ! $(command -v docker) ] && xdebug off || ddev xdebug off", 
			"presentation": {
				"reveal": "silent",
				"close": true
			}
		}
	]
}
